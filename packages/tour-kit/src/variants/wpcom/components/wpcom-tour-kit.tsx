import TourKit from '../../../components/tour-kit';
import '../styles.scss';
import type { WpcomStep } from '../../..';

const foo: WpcomStep = {
	meta: {
		bar: 123,
		foo: 'wer',
	},
};

foo;

export default TourKit;
